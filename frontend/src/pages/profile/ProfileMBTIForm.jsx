import React from 'react'

const ProfileMBTIForm = ({ user, followingRanks, followerRanks }) => {
    return (
        <div>
            <h2>MBTI Form for {user.username}</h2>
            {/* 예시 데이터 표시 */}
        </div>
    );
};

export default ProfileMBTIForm